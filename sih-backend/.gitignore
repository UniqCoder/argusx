# ── Secrets — NEVER commit these ────────────────────────────────────────────
.env
*.env
!.env.example

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.pyo

# ── Virtual envs ─────────────────────────────────────────────────────────────
venv/
env/
ENV/
.venv/
.env.local

# ── ML artifacts & Datasets ──────────────────────────────────────────────────
backend/app/ml/artifacts/*.pkl
backend/app/ml/artifacts/*.joblib
backend/app/ml/artifacts/*.model
backend/app/ml/artifacts/*.npz
# ...EXCEPT the 3 files actually required for live /risk inference. These were
# previously excluded on the theory they're "regenerable via train.py", but
# that requires the Elliptic++ / Kaggle training datasets, which are NOT in
# this repo and never will be (multi-GB external downloads, see docs/ml.md) —
# so on any fresh clone/deploy (e.g. Railway) there is in fact no way to
# regenerate them. Without these committed, /risk silently degrades to
# risk_tier=unknown for every wallet on a fresh deploy, which is a much worse
# outcome than committing ~20MB of trained-model binary.
!backend/app/ml/artifacts/risk_model.joblib
!backend/app/ml/artifacts/wallet_embeddings.npz
!backend/app/ml/artifacts/relative_features_reference.json
data/
data/raw/
data/processed/
backend/data/
# curated OFAC sanctions seed is committed source data (not generated corpus)
!backend/app/data/
!backend/app/data/sanctions_seed.json
*.csv.gz
*.tar.gz

# ── Download artifacts ───────────────────────────────────────────────────────
*.part

# ── EDA output (generated, not source) ───────────────────────────────────────
eda_output/

# ── Alembic auto-generated (only commit intentional migrations) ───────────────
# Don't add specific revision files here — all are committed intentionally

# ── Testing / coverage ────────────────────────────────────────────────────────
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.lcov
scratch/
backend/scratch/

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# ── Docker ───────────────────────────────────────────────────────────────────
# Don't ignore docker-compose files — they're config, not secrets
# Volume data (if mounted locally)
postgres_data/
neo4j_data/
redis_data/

# ── Node (frontend — not backend scope, but good to have in monorepo) ────────
node_modules/
.next/
dist/
.turbo/
frontend/
